• 关于“自动调节核心 + 低层优先”的可实现方式

  - 可用核心获取：在 Python 里直接用 os.sched_getaffinity(0) 或 psutil.Process().cpu_affinity() 获取当前进程可用 core 集合（在
    numactl --physcpubind=0-95 下会返回 0–95），无需手动参数。
  - 核心分配策略（建议）
      - 建一个全局 KmeansScheduler，维护“待聚类层”的优先队列（layer_idx 越小优先级越高）。
      - 限制 同时运行的 K‑means 层数，让每个层获得更多线程；比如根据模型结构自动计算：min_threads_per_job = kv_heads *
        subspace_parts，max_concurrent = max(1, cores // min_threads_per_job)，这样不需要手调参数。
      - 每个任务启动时动态设置 worker_threads = cores // active_jobs（或按 layer_idx 加权），完成后释放名额，后续层自动获得更多
        线程。
  - 落地点（当前代码需要支持动态线程数）
      - worker_threads_ 是在 start_chunk_pipeline 时固定的（determine_worker_threads），之后 K‑means 用这个值。library/
        AdaptiveIMI/cpp_extensions/src/layer_pipeline.cpp:443
      - 如果要“动态调整”，需要加一个 set_worker_threads(int) 或在 merge_all_chunks 前重算并覆盖 worker_threads_，否则 Python 侧调
        度也无法改变每层线程数。library/AdaptiveIMI/cpp_extensions/src/layer_pipeline.cpp:1175

  关于 cache_hub/retroinfer_cache_imi.py:589 的阻塞影响

  - 这里的 copy_done_event.synchronize() 会让该层的 consumer 线程阻塞等待 D2H 完成，直到完成才继续提交到 C++ pipeline。cache_hub/
    retroinfer_cache_imi.py:589
  - 同时，free_slots.put(slot_id) 只会在 worker 处理完后执行，意味着 GPU 端如果 stage_slots 用尽，就会被迫等待（chunk 流水线“深
    度”被压成 1~2）。cache_hub/retroinfer_cache_imi.py:637
  - 结果是 GPU 计算 → D2H → 提交 → 下一块 被串成一条“同步链”，减少了 CPU/GPU overlap，容易出现 GPU 等待、CPU 等待。

  如果“让 C++ pipeline 直接做 D2H”的影响

  - 好处：
      - Python 侧不再同步等待事件，提交更快，in‑flight chunk 增多，CPU/GPU overlap 提升。
        保存 torch::Tensor（或 Python 保持引用直到 D2H 完成）。library/AdaptiveIMI/cpp_extensions/src/layer_pipeline.cpp:686
      - 当前 streaming 路径用 CPU pinned stage buffer 是为了避免 GPU tensor 生命周期问题；如果取消 staging，就必须把“生命周期管
        理”下沉到 C++ queue。

  结论（针对你现在的目标）

  - 真正拖慢的是 K‑means 的并发稀释，不是 D2H；你需要一个“低层优先 + 限制并发层数”的调度器，并能动态调整 worker_threads。
  - copy_done_event.synchronize() 会降低流水线深度，确实会拖慢整体 overlap，但它只是“其次要优化”的点；它更像是加重尾延迟。

  如果你愿意，我可以：

  - 帮你设计一个“自动、低层优先”的 K‑means 调度器方案（含 C++ set_worker_threads 接口草案）。
  - 或者直接帮你改掉 copy_done_event.synchronize() 的提交路径，让 C++ pipeline 接管 D2H，同时补齐 GPU tensor 生命周期管理。

  • 关于“自动调节核心 + 低层优先”的可实现方式

  - 可用核心获取：在 Python 里直接用 os.sched_getaffinity(0) 或 psutil.Process().cpu_affinity() 获取当前进程可用 core 集合（在
    numactl --physcpubind=0-95 下会返回 0–95），无需手动参数。
  - 核心分配策略（建议）
      - 建一个全局 KmeansScheduler，维护“待聚类层”的优先队列（layer_idx 越小优先级越高）。
      - 限制 同时运行的 K‑means 层数，让每个层获得更多线程；比如根据模型结构自动计算：min_threads_per_job = kv_heads *
        subspace_parts，max_concurrent = max(1, cores // min_threads_per_job)，这样不需要手调参数。
      - 每个任务启动时动态设置 worker_threads = cores // active_jobs（或按 layer_idx 加权），完成后释放名额，后续层自动获得更多
        线程。
  - 落地点（当前代码需要支持动态线程数）
      - worker_threads_ 是在 start_chunk_pipeline 时固定的（determine_worker_threads），之后 K‑means 用这个值。library/
        AdaptiveIMI/cpp_extensions/src/layer_pipeline.cpp:443
      - 如果要“动态调整”，需要加一个 set_worker_threads(int) 或在 merge_all_chunks 前重算并覆盖 worker_threads_，否则 Python 侧调
        度也无法改变每层线程数。library/AdaptiveIMI/cpp_extensions/src/layer_pipeline.cpp:1175

  关于 cache_hub/retroinfer_cache_imi.py:589 的阻塞影响

  - 这里的 copy_done_event.synchronize() 会让该层的 consumer 线程阻塞等待 D2H 完成，直到完成才继续提交到 C++ pipeline。cache_hub/
    retroinfer_cache_imi.py:589
  - 同时，free_slots.put(slot_id) 只会在 worker 处理完后执行，意味着 GPU 端如果 stage_slots 用尽，就会被迫等待（chunk 流水线“深
    度”被压成 1~2）。cache_hub/retroinfer_cache_imi.py:637
  - 结果是 GPU 计算 → D2H → 提交 → 下一块 被串成一条“同步链”，减少了 CPU/GPU overlap，容易出现 GPU 等待、CPU 等待。

  如果“让 C++ pipeline 直接做 D2H”的影响

  - 好处：
      - Python 侧不再同步等待事件，提交更快，in‑flight chunk 增多，CPU/GPU overlap 提升。
  - copy_done_event.synchronize() 会降低流水线深度，确实会拖慢整体 overlap，但它只是“其次要优化”的点；它更像是加重尾延迟。

  1. kmeans_execute 入口：校验 dtype → KmeansConcurrencyGuard 计数并发层数 → 计算线程预
     算。library/AdaptiveIMI/cpp_extensions/src/kmeans_core.cpp:862 library/AdaptiveIMI/
     cpp_extensions/src/kmeans_core.cpp:883
  2. compute_kmeans_thread_budget & maybe_log_kmeans_budget：根据 active_kmeans 缩放
     effective_threads 和 effective_min_threads。library/AdaptiveIMI/cpp_extensions/src/
     kmeans_core.cpp:92 library/AdaptiveIMI/cpp_extensions/src/kmeans_core.cpp:108
  3. FP16/BF16 → FP32 分块转换（OpenMP 并行）。library/AdaptiveIMI/cpp_extensions/src/
     kmeans_core.cpp:946
  4. KmeansExecutor::execute（subspace=2 主体）：
      - extract_subvectors 并行提取前半/后半子空间。library/AdaptiveIMI/
        cpp_extensions/src/kmeans_core.cpp:335 library/AdaptiveIMI/cpp_extensions/src/
        kmeans_core.cpp:477
      - #pragma omp parallel for 并行执行 head×phase 任务（每个任务调用
        run_imi_single_phase_from_subvec）。library/AdaptiveIMI/cpp_extensions/src/
        kmeans_core.cpp:488
  5. run_imi_single_phase_from_subvec → kmeans_plusplus_init_inplace → lloyd_iteration。
     library/AdaptiveIMI/cpp_extensions/src/kmeans_core.cpp:373 library/AdaptiveIMI/
     cpp_extensions/src/kmeans_core.cpp:146 library/AdaptiveIMI/cpp_extensions/src/
     kmeans_core.cpp:200
  6. lloyd_iteration 内部：
      - E‑step：#pragma omp parallel for num_threads(thread_count)。library/AdaptiveIMI/
        cpp_extensions/src/kmeans_core.cpp:206
      - M‑step：#pragma omp parallel num_threads(thread_count) + #pragma omp for。
        library/AdaptiveIMI/cpp_extensions/src/kmeans_core.cpp:280
      - 距离计算用 euclidean_distance_avx2。library/AdaptiveIMI/cpp_extensions/src/
        kmeans_core.cpp:25



  现状量化（来自日志 + 代码）

  - 日志显示 active=6~8 时 effective_threads=12~16，而 min_threads_per_phase=6。
    effective_threads = worker_threads / active_kmeans 来自
    compute_kmeans_thread_budget。library/AdaptiveIMI/cpp_extensions/src/
    kmeans_core.cpp:90
  - K‑means 外层并行度：
    max_concurrent_phases = min(total_tasks, effective_threads / min_threads_per_phase)。
    代入 effective_threads=12、min_threads_per_phase=6 → 只能并行 2 个任务，每层最多 12
    线程。library/AdaptiveIMI/cpp_extensions/src/kmeans_core.cpp:463
  - 这解释了你看到的 CPU 利用率低（每层最多 12 线程），即使总核数 96。

  ———

  ## 可行调整方案（只影响并行性）

  ### 方案 A：限制同时进入 K‑means 的层数（关键）

  目标是把 active_kmeans 控制在 2~3，这样 effective_threads 变成 32~48。

  - 例如 active=2 → effective_threads=48
    → max_concurrent_phases = 48/6 = 8（每层并行 8 个任务，48 线程）
  - 预期：单层 K‑means wall time 约 3~4× 缩短，总耗时有机会落到 <4s。

  为何只影响并行性

  - 只是调度“谁先跑 K‑means”，算法完全不变。
  - 控制点在 K‑means 启动前（finish_index），而非 K‑means 内部逻辑。cache_hub/
    retroinfer_cache_imi.py:617

  ### 方案 B：降低 min_threads_per_phase 限制

  目前 min_threads_per_phase=6，对 effective_threads=12~16 时并行度压得非常死。

  - 调整为 3 或 4，能把外层并行任务数翻倍：
    effective_threads=12 → max_concurrent_phases=12/3=4。
  - 不改变算法，仅改变任务划分。
  - 关键设置在 runtime_config.min_threads_per_phase（最终进入 kmeans_execute）。library/
    AdaptiveIMI/cpp_extensions/include/layer_pipeline.hpp:142 library/AdaptiveIMI/
    cpp_extensions/src/kmeans_core.cpp:463

  ### 方案 C：固定 CPU 绑定，防止线程迁移

  如果 NUMA 或线程迁移严重，会降低 OpenMP 实际利用率。

  - 运行前设置：
    OMP_PROC_BIND=spread
    OMP_PLACES=cores
  - 只影响线程调度，不改算法。

  ### 方案 D：确保全核绑定（非常重要）

  你的脚本里 record 阶段是 --physcpubind=0-23（只有 24 核），如果结果来自 record，它天然
  低利用率。

  - 请确认你测试的是 warmup 还是 record。throughput_eval/run_decode_breakdown.sh:72

  ———

  ## 推荐组合（最有可能把 32k 聚类压到 <4s）

  1. 限制 active_kmeans ≤ 2 或 3（优先级最高）。
  2. min_threads_per_phase 调到 3 或 4。
  3. 开启 OMP_PROC_BIND / OMP_PLACES。

  如果你愿意，我可以根据你目标（32k < 4s）给出一个自动计算并发上限的规则（只影响调度，不
  改算法），并结合你 CPU/NUMA 结构调优线程绑定策略。